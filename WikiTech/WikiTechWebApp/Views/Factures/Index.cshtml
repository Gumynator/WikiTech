@model IEnumerable<WikiTechAPI.Models.Facture>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MontantFacture)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateFacture)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TitreFacture)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MontantFacture)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateFacture)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TitreFacture)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.IdFacture">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdFacture">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdFacture">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
